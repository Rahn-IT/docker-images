kind: pipeline
type: docker
name: default

steps:
- name: test
  image: docker
  privileged: true
  commands:
  - apk update
  - apk add git
  - echo $DRONE_COMMIT
  - git diff $DRONE_COMMIT_BEFORE
  